package jrails;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class Test {
//    public static void main(String[] args) {
//        System.out.println(Controller.class.getName().split("\\.")[1]);
//        Map<String, String> params = new HashMap<>();
//        System.out.println(Map.class);
//        Student b = new Student();
//        b.name = "Programming Languages: Build  Prove  and Compare";
//        b.address = "Norman Ramsey";
//        b.age = 999;
//// The book b exists in memory but isn't saved to the db
//        b.save(); // now the book is in the db
//        System.out.println("save studnet");
//        b.age = 42; // the book in the db still has 999 copies
//        b.save(); // now the book in the db has 42 copies
//        Student b2 = new Student();
//        b2.name = "Peter Park";
//        b2.address = "Empress street";
//        b2.age = 16; // hm, same as other book
//        b2.save(); // a second record is added to the database
//        Student b3 = new Student();
//        b3.name = "CLRS";
//        b3.address = "unknown";
//        b3.age = 77; // hm, same as other book
//        b3.save(); // a second record is added to the database
//        assert(b.id() != b2.id()); // every row has a globally unique id (int) column, so we can tell them apart
//        Student b3_1 = Model.find(Student.class, 3); // finds the book with id 3 in the db, if any
//        System.out.println("b3_1 is:"+b3_1.name+",,id:"+b3_1.id());
//        List<Student> bs = Model.all(Student.class); // returns all books in the db
//        System.out.println("all student size are:"+bs.size());
//        b.destroy(); // remove book b from db
//        List<Student> bs2 = Model.all(Student.class); // returns all books in the db
//        System.out.println("After destroy, all student size are:"+bs2.size());
//    }
}
